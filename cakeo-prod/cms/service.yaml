apiVersion: v1
kind: Service
metadata:
  name: cms-prod
  namespace: football-live68-prod
spec:
  type: ClusterIP
  selector:
    app: cms-prod
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
