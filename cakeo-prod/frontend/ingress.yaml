apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-prod
  namespace: football-live68-prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  rules:
  - host: cakeo.live
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-prod
            port:
              number: 80
